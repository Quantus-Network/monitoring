apiVersion: 1

policies:
  - orgId: 1
    receiver: Email Notifications
    group_by: ["alertname", "grafana_folder", "instance"]
    group_wait: 30s
    # Notify once per incident: no update/repeat storms while still firing
    group_interval: 8736h
    repeat_interval: 8736h
